export type DataFromServerTypes = {
    address: {
        city: string
        street: string
        zipCode: number
    }
    company: {
        name: string
    }
    name: string
    userName: string
    email: string
    phone: number
    website: string
}
